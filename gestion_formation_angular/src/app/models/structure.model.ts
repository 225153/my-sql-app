/**
 * Modèle TypeScript représentant la Structure d'un participant.
 * Idéal pour garantir des typages statiques forts, évitant bcp de crashs en runtime.
 */
export interface Structure {
  id?: number;
  libelle: string;
}
